#ifndef CONFIG_H
#define CONFIG_H

const QString SDDM_CONFIG_FILE = "/etc/sddm.conf";
const QString HALT_COMMAND = "/sbin/shutdown -h -P now";
const QString REBOOT_COMMAND = "/sbin/shutdown -r now";
#endif